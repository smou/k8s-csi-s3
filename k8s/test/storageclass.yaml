apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: s3-standard
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: minio.s3.csi
parameters:
  region: us-east-1
reclaimPolicy: Delete
volumeBindingMode: Immediate
allowVolumeExpansion: false
